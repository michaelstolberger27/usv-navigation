# A single dynamic obstacle in the ego's local metric frame.
# x east, y north (metres); velocity in m/s; heading in radians CCW from +x.
# Matches the (x, y, v, psi) tuple the automaton consumes.
float64 x
float64 y
float64 velocity
float64 heading
